describe('src/listing tickets suite', () => {
  describe('Given graphql is working', () => {
    it('when I list all tickets it works properly', () => {
      console.log(`>>>>>>>>>>>>`, process.env.NODE_ENV);
      return Promise.resolve(true);
    });
  });
});
